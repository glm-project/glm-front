// Replaced at build time by the --define of `npm run deployed:build:pupitre`, which reads the
// DEPLOYED_KEYCLOAK_URL of the deployment. Only environment.deployed.ts names it, and only the
// `deployed` configuration of build-pupitre substitutes that file.
declare const NG_DEPLOYED_KEYCLOAK_URL: string;
